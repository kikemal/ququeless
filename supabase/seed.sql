-- Development seed (optional). Not applied as production data.
-- Usage: supabase db reset   (runs migrations, then this file if configured)
-- Keep empty for Phase 1 — no fake businesses, queues, or customers.

SELECT 1;
